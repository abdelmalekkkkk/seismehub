type PopulationOption = {
    from: number;
    to: number;
    label: string;
}

type NeedOption = {
    id: string;
    label: string;
}

type LevelOption = {
    id: string;
    label: string;
}

type HelpedOption = {
    id: string;
    label: string;
}